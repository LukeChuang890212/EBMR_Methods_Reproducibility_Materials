res <- readRDS("Simulation_Results/EBMR_IPW_setting4-miss50-scenario9-3_13_n2000_replicate1000_test59.RDS")
cat("dim:", dim(res), "\n")
cat("rownames:\n")
print(rownames(res))
cat("valid row1:", sum(!is.na(res[1,])), "\n")
mu <- res[1, !is.na(res[1,])]
se <- res[3, !is.na(res[1,])]
cat("mu mean:", mean(mu), " sd:", sd(mu), "\n")
cat("se mean:", mean(se), " median:", median(se), "\n")
cat("mu quantiles:\n")
print(quantile(mu, c(0, 0.01, 0.05, 0.5, 0.95, 0.99, 1)))
cat("se quantiles:\n")
print(quantile(se, c(0, 0.01, 0.05, 0.5, 0.95, 0.99, 1)))
cat("|mu| > 5:", sum(abs(mu) > 5), "\n")
cat("se > 1:", sum(se > 1), "\n")
